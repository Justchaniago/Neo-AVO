# Neo AVO Mobile v1 Documentation Pack

This folder is the implementation contract for the Neo AVO mobile-native UX project.

Start with `00_MASTER_PLAN.md`, then read `13_AGENT_EXECUTION_CONTRACT.md`.

Desktop is a protected baseline. Mobile uses the same design language but a dedicated operational composition and interaction model.
